---
req_id: REQ-001
title: "项目脚手架 — docs/tasks/harness 目录结构"
status: draft
owner: unassigned
priority: P0
phase: PHASE-001
scope: docs
tc_policy: exempt
tc_exempt_reason: "纯目录结构和配置变更，无运行时逻辑可测试"
depends_on: []
test_case_ref: []
acceptance: "运行 pnpm typecheck 不报错；运行 pnpm lint 退出码为 0（基线清理后）；tasks/features/ tasks/phases/ harness/ docs/adr/ docs/arch/ 目录均存在；package.json 中 scripts.typecheck 字段为 tsc --noEmit"
review_round: 0
pending_bugs: []
blocked_reason: ""
blocked_from_status: ""
blocked_from_owner: ""
pr_number: ""
---

# Goal

建立 OpenStock 迁移工程所需的目录脚手架，使后续 REQ 文件、测试用例、ADR 文档有正确的存储位置，并确保 `pnpm typecheck` CI 命令可用。

# In Scope

- 创建目录：`tasks/features/`, `tasks/phases/`, `tasks/bugs/`, `tasks/test-cases/`, `tasks/archive/done/`
- 创建目录：`harness/`, `docs/adr/`, `docs/arch/`
- `package.json` scripts 新增：
  - `"typecheck": "tsc --noEmit"`
- `tsconfig.json` 确认 `strict: true` 已启用
- **Lint 基线清理**：修复原始仓库存量的约 36 个 eslint errors（不要求消除 warnings），使 `pnpm lint` 退出码为 0

  pre-existing errors 分布（按文件）：
  - `scripts/check_db_name.js`, `scripts/resolve_srv.js`：`@typescript-eslint/no-require-imports`（require() 语法）
  - `app/(root)/about/page.tsx`：`no-unescaped-entities`, `no-explicit-any`
  - `__tests__/utils.test.ts`：`no-explicit-any`
  - `lib/inngest/functions.ts`：多处 `no-explicit-any`
  - `lib/kit.ts`：`no-explicit-any`
  - 其他若干文件：`no-explicit-any`

# Out of Scope

- 任何功能代码
- `.github/workflows/` CI 文件（REQ-011）
- Lint warnings（47 个 warnings 不影响 CI 退出码）

# Acceptance Criteria

- `ls tasks/features/ tasks/phases/ harness/ docs/adr/ docs/arch/` 无报错
- `pnpm typecheck` 命令存在且通过（0 errors）
- `pnpm lint` 退出码为 0（errors 清零；warnings 允许存在）
- `tsconfig.json` 包含 `"strict": true`
